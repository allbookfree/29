/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "*.picard.replit.dev",
    "*.janeway.replit.dev",
    "*.replit.dev",
    "*.repl.co",
  ],
};

export default nextConfig;
